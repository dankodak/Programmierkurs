import interest
import math

import interest
import math

print("Nach {} Jahren, beträgt der Betrag {:.2f} €"
      .format(10, interest.verzinsung(1000, 0.1, 10*360)))
print("Die Anzahl der Jahre beträgt {} Jahre."
      .format(math.ceil((interest.anzahl_tage(1000,2000,0.1))/360)))

